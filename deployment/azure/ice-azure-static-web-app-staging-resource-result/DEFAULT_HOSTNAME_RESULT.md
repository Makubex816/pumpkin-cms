# Default Hostname Result

Generated: 2026-06-06

## Hostname

Azure assigned the staging default hostname:

```text
happy-mud-0b375e20f.7.azurestaticapps.net
```

This hostname is the first approved validation host for a future staging deployment.

No custom domain was configured. No DNS or Cloudflare records were changed.

## Form-Origin Note

The latest verified static output already points the contact form UI at:

```text
https://func-ice-static-contact-20260605.azurewebsites.net/api/static-contact
```

Browser submission from the new Azure Static Web Apps default hostname may require a separate Function allowed-origin setting update. This run did not approve or perform that setting change.
