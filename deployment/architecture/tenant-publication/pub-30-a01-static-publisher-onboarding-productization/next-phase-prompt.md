# Next safe gate

Do not begin PUB-40 or any PUB-30 deployment/synthetic continuation from this package.

First obtain explicit `PLATFORM_SECRET_ROTATION_AND_PARITY_RECOVERY` owner authority after the PUB-30 read-only diagnostic exposed protected API configuration values in command trace. The recovery must:

1. identify affected secret classes without printing values;
2. rotate and invalidate them using approved providers and secure handoff;
3. restore API/Admin/starter configuration parity without changing customer data, domains, indexing, capacity, or paid resources;
4. deploy only the exact accepted frozen packages within a newly stated attempt budget;
5. prove health/readiness, approved-role identity, tenant isolation, Forms inbox/public forms, retained synthetic state, customer runtimes, deployment identities, and zero plaintext;
6. preserve the accepted local/two-clean-root evidence and reconcile the exact result and Atlas authority commits without rerunning PUB-30 productization.

After that gate closes, the prepared PUB-40 prompt may be activated only with an explicit owner-selected customer tenant and customer-mutation budget. Ice Rink Rentals is the evidence-backed safest first candidate but is not selected by this package.

Parallel/retained relationships remain PERF-10 and IDM-40. DOM-20 remains held. SEC-20 is conditional. PAY-00 remains blocked.
