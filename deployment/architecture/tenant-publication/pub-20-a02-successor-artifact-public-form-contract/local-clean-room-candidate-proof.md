# Local clean-room candidate proof

Root npm restore/build/type checks, starter production build, API Release builds with and without Mongo, focused tests, backup/restore tests, onboarding dry-run, and root CI passed.

Two clean roots produced identical ten-file candidate inventories and hashes. JSON, POSIX archive paths, completeness, secret, customer-payload, raw-log, and absolute-path checks passed.
