# Noindex runtime proof

Root, contact, robots, sitemap, tenant manifest, and public client returned 200 from the default HTTPS host with bodies matching the frozen files.

Pages and responses expose noindex,nofollow,noarchive; robots disallows all; sitemap has no URL; and staticwebapp.config.json is not disclosed.
