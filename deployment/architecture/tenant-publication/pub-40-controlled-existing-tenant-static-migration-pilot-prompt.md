# TASK ID PUB-40-A01 — Controlled existing-tenant static migration pilot

Status: `HELD_SECURITY_RECOVERY_LEGAL_REVIEW_OWNER_TENANT_SELECTION`.

## Purpose

Migrate one owner-selected existing tenant through the accepted PUB-30 product flow with exact backup, preview, publication, rollback, and customer no-regression proof. PUB-30 evidence recommends Ice Rink Rentals as the safest first candidate, but this prompt does not select it.

## Activation gates

Do not execute until all are true:

1. the PUB-30 source, two clean roots, deterministic candidates, Atlas/working-memory successor, and repository closeout are complete;
2. the protected-value command-trace incident has an explicitly authorized `PLATFORM_SECRET_ROTATION_AND_PARITY_RECOVERY` closeout with authoritative production readback;
3. owner/legal review authorizes distribution of the exact LICENSE/NOTICE package;
4. the owner names exactly one tenant and approves its exact candidate hashes, adaptations, form mode, customer mutation budget, deployment budget, backup/rollback package, and cost;
5. domains/DNS/TLS and indexing remain separately held unless independently authorized.

## Bounded execution

- freeze and checksum pre-migration customer state and current runtime;
- regenerate the selected candidate twice from the exact accepted source commit;
- require identical inventories, artifact/manifest hashes, routes, media, redirects, forms, attribution, and held noindex state;
- create immutable product release/artifact/job records for only the selected tenant;
- deploy preview/no-post first through the approved secret provider;
- prove identity, tenant isolation, form preflight, customer content/runtime fidelity, and rollback;
- issue a customer form POST only with an explicit logical-submission and HTTP-POST budget;
- promote only after owner acceptance;
- preserve and prove exact rollback to the predecessor;
- make no domain, DNS, TLS, indexing, capacity, paid-plan, Airstrip, payment, or unrelated customer change.

## Required closeout

Report exact source and deployment commits, package hashes, attempts, before/after customer digests, release/publication/job lineage, rollback proof, form accounting, security-provider evidence without values, Atlas/working-memory update, and remaining owner holds.
