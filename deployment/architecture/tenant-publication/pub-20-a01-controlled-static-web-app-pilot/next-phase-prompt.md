# Next prompt: PUB-20 focused correction authority

Authorize a PUB-20 recovery only if the owner accepts all of the following:

1. Preserve the INT-10 artifact and hash as immutable baseline evidence.
2. Create a separately versioned deterministic successor artifact with technical noindex, the real shared API origin, a runnable submit client, consent, honeypot, and safe tenant/release/form identifiers.
3. Implement a narrowly scoped anonymous shared-API contract that independently validates active tenant, release, exact Origin, active mapped form, fields, consent, honeypot, submission/correlation UUIDs, bound idempotency, rate limits, and tenant isolation.
4. Authorize focused tests and one bounded API deployment with rollback evidence.
5. Authorize deployment of the successor artifact to one Free SWA on the Azure-generated hostname while retaining all domain/customer/Airstrip/indexing/capacity prohibitions.

PUB-30 remains held until that corrected PUB-20 proof succeeds.
