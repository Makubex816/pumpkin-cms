# Secure token use proof

Secure successor-token use was proven for the bounded Static Web App deployments. It was decrypted into memory, supplied only through the child environment, never placed on the command line, never printed with the environment, and cleared afterward.

SWA deployment count: 2. API deployments: 0. Customer deployments: 0. The token remains subject to the same-user-profile portability hold.
