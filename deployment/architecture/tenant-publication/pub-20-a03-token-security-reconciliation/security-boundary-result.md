# Security boundary result

No secret, deployment token, DPAPI protected payload, ciphertext, entropy, password, signing key, ticket, customer payload, raw log, browser profile, or absolute host path is included.

The repository result and distributable packages contain only safe identifiers, hashes, counts, statuses, relative references, and conclusions.
