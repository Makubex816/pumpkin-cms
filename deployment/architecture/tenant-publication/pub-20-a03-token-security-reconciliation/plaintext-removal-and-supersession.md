# Plaintext removal and supersession

The legacy plaintext handoff is absent and superseded by the restricted DPAPI CurrentUser envelope. The predecessor Azure token was invalidated by reset. The A02 metadata status is SUPERSEDED_PLAINTEXT_TOKEN_INVALIDATED.

No active pointer, package, result document, or closeout evidence contains the token value or protected payload.
