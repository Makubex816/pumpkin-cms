# Legacy plaintext token assessment

Legacy plaintext token present: false. Plaintext token file count: 0. Token value printed: false. Token value committed: false.

Only nonsecret fingerprints, envelope metadata, resource identity, and hashes are recorded. No token, ciphertext, entropy, or DPAPI payload is packaged.
