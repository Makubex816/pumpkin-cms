# Google Indexing Deferred Carryforward

Status: carried forward by hard stop.

V2.9.8 did not run:

- Google Search Console;
- sitemap submission through Google;
- URL Inspection API;
- Google Indexing API;
- indexing request;
- crawl;
- outbound live URL check.

Future API responses must preserve `indexingState: deferred` and expose Google indexing as a deferred next gate only.

