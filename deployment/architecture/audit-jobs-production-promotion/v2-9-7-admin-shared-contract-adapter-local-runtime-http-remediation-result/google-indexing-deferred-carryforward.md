# Google Indexing Deferred Carryforward

Status: carried forward by hard stop.

Google/Search Console/indexing remains deferred. V2.9.7 did not run:

- Search Console actions;
- sitemap submission through Google;
- URL Inspection API;
- Google Indexing API;
- indexing request;
- crawl;
- outbound live URL checks.

The Admin adapter preserves `indexingState: deferred` from the shared viewer model.

