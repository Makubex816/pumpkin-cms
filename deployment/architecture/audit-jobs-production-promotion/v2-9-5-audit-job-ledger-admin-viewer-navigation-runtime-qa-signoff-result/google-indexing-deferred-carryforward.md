# Google Indexing Deferred Carryforward

Status: deferred hard stop preserved.

V2.9.5 did not run Google Search Console, sitemap submission through Google, URL Inspection API, Google Indexing API, indexing requests, crawl checks, or outbound live checks.

The Admin viewer continues to display:

- `Google/Search Console/indexing deferred hard stop`.
- `Indexing Deferred`.
- disabled `Request indexing` future action.

Indexing remains a future separately approved boundary.
