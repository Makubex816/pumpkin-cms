# Next Phase Prompt

Use this prompt only when intentionally starting the next approval phase.

```text
Approve V2.9.3 Audit Job Ledger Read-Only Operator Viewer Planning only: use the completed V2.9.2 Audit Job Ledger No-Write Validator Foundation package to plan the first read-only operator viewer/dashboard model for local audit/job/promotion ledgers. Review the V2.9.2 validator schemas, CLI output shape, valid/invalid fixtures, V2.9.1 operator dashboard requirements, and V2.8 carryforward evidence. Produce a read-only viewer requirements packet, route/component or CLI report design if local-only, dashboard state model, fixture-to-view mapping, safety boundary summary, test plan, result package, root report, and updated control docs. Do not deploy, redeploy, change DNS, alter custom domains, run Google Search Console, submit sitemaps through Google, use URL Inspection API, use Google Indexing API, request indexing, crawl, follow outbound links, submit contact forms, POST to contact endpoints, perform CMS writes, perform provider writes, mutate Azure infrastructure/configuration/app settings, assign RBAC, read protected config, use/print/export/list deployment or OAuth tokens, query Key Vault secrets, use keys/listKeys, generate connection strings, generate SAS, or use git add -A.
```
