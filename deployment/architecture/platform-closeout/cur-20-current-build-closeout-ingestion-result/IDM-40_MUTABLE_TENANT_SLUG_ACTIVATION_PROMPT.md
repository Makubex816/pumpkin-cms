# IDM-40 mutable tenant slug activation prompt

Precondition: IDM-40 is retained as the downstream continuation, but it is not authorized by the blocked CUR-20 result. Use only after the owner selects the downstream path and the active Atlas/working-memory package names IDM-40 as the current gate.

```text
TASK ID: IDM-40 — Mutable Tenant Slug Activation

Repository:
C:\Users\User\Desktop\PumpkinCMS\pumpkin-cms

Branch:
feature/admin-page-editor-import-export

Carryforward:
- IDM-30 / V2.8.63CRSTUR completed identity-management production activation.
- Identity foundation, dual-read, dual-write, and management stages are active.
- Tenant rename remains disabled.
- Migration execution remains disabled.
- External notification provider remains disabled.
- S2 / two workers is retained only as a temporary observation decision under PERF-10.
- Email provider and forgot-password delivery remain held pending provider configuration.
- Airstrip public runtime and indexing remain frozen.

Objective:
Plan and, only when explicitly authorized, execute controlled mutable tenant slug activation without customer credential, role, membership, contact, form, FormEntry, domain, DNS, indexing, or Airstrip runtime regression.

Required before any mutation:
- active Atlas and working-memory package are reconciled after CUR-20;
- exact tenant/synthetic pilot target is authorized;
- rollback and audit plan exists;
- customer data preservation readback is fresh;
- tenant rename flag remains off until the hard gate opens;
- no migration execution or external provider action is smuggled into IDM-40.

Boundaries:
No upstream merge, no payment work, no capacity change, no Airstrip public runtime request, no indexing action, no unbounded customer tenant rename.
```

