# Stale Binding Check Result

Status: passed.

The V2.5.1 validator checks evidence references in:

- top-level evidence references
- provider profile evidence references
- resource binding evidence references

Validation result:

- Failures: `0`
- Warnings: `0`
- Evidence refs checked: `21`

The first run found one filename mismatch for the Runtime QA package, which was corrected from `result-manifest.json` to `manifest.json`.

