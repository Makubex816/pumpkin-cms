# Next Phase Prompt

Approve V2.8.17D Production Deploy Working-Directory Separation Corrective Execution only: use the completed V2.8.17C command-shape result to perform one final corrected production static artifact deployment attempt for IceSkatingRinkRentals.com only after rebuilding and validating a fresh sanitized artifact. The blocker is now known to be the artifact-root working-directory shape: V2.8.17C fixed `SWA_CLI_DEPLOY_DRY_RUN=false` and `DEPLOYMENT_ACTION=upload`, but StaticSitesClient rejected `deploy .` from inside the artifact root because the current directory was identical to the artifact folder. Do not repeat token reset, token-target comparison, VS Code restart, or `swa deploy --dry-run`. Do not deploy from the artifact root. Use `SWA_CLI_DEPLOYMENT_TOKEN` by environment-variable reference only and never print, list, export, log, commit, write, or reveal the token. Reconfirm target `swa-ice-static-staging` in `rg-ice-static-staging` and Ready custom domains by safe read-only Azure metadata. If all gates pass, execute exactly one production deployment from a working directory outside the artifact folder, using the selected artifact folder as the deploy target, for example from the sanitized app root with `npx --yes @azure/static-web-apps-cli@2.0.9 deploy out --app-name "swa-ice-static-staging" --resource-group "rg-ice-static-staging" --env production --no-use-keychain`. Do not broadly retry. If deployment succeeds, run only six bounded GET checks for `/`, `/service-areas`, and `/contact` on `iceskatingrinkrentals.com` and `www.iceskatingrinkrentals.com`. Do not change DNS, custom domains, app settings, RBAC, Azure infrastructure beyond the single static artifact deployment, Search Console/indexing, CMS/provider data, contact forms, or contact endpoint POSTs. Do not read protected config, `.env.local`, Key Vault secrets, keys/listKeys, connection strings, or SAS. Produce the result package, root report, tracker/SOT updates, and exact-path commit instructions.

