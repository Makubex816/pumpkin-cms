# Pumpkin API Build Test Result

Result: pass.

Commands completed before deployment:

- `dotnet run --project apps/pumpkin-api.Tests/pumpkin-api.Tests.csproj -- --v2-8-42-mediaasset`: pass.
- `dotnet build apps/pumpkin-api/pumpkin-api.csproj --no-restore`: pass.

Build warnings: `0`.

Build errors: `0`.

