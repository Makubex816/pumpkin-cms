# Isolated Contact Page Preflight Result

Approved check:

`GET https://kind-island-0a85a740f.7.azurestaticapps.net/contact`

Result:

| Field | Value |
| --- | --- |
| Status | `200` |
| Contains `/api/static-contact` | true |
| Contains `contact@iceskatingrinkrentals.com` | true |
| Static endpoint serialized as `/api/contact` | false |
| Response length | 70685 |

The page preflight passed, so the single approved isolated synthetic POST was sent.
