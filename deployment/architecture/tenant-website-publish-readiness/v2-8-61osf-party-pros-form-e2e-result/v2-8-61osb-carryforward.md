# V2.8.61OSB Carryforward

OSB is committed at `d3b1eb8d` with message `Repair Party Pros media rendering`.

Carryforward accepted by OSF:

- Party Pros media rendering repair completed before this phase.
- Custom HTTPS pages rendered expected image/media references in OSF prerecheck.
- No media upload batch, container deletion, container recreation, storage key, listKeys, or SAS action occurred in OSF.

The Party Pros custom and `www` routes returned HTTP 200 for `/`, `/contact`, and `/service-areas` during OSF prerecheck and final no-regression.

