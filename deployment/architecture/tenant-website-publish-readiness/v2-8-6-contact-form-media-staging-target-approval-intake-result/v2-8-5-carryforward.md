# V2.8.5 Carryforward

V2.8.5 closed the missing repo-supported sanitized no-dotenv build path.

Carryforward facts:

- Script: `apps/ice-rink-web/scripts/sanitized-static-build.mjs`
- Command: `npm run build:static:ice:sanitized`
- Evidence path: `apps/ice-rink-web/.tmp/sanitized-static-build/`
- Protected config copied: `false`
- Protected config contents read: `false`
- Deployment/DNS/indexing/live publication remained closed.

V2.8.6 revalidated the same path with run:

```text
sanitized_20260612151525
```
