# Public FormDefinition Read Result

Result: passed.

Endpoint shape:

- `GET /api/forms/{tenantId}/definitions/{type}`

Readback:

- Public FormDefinition read for `ice-rink-rentals` and `v2-8-49-ui-proof-form`: HTTP 200.
- V2.8.49 trace present: true.
- Updated marker present: true.

The tenant API key was used only in memory and was not printed or written.
