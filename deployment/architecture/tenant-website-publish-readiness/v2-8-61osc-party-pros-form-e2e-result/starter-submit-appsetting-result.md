# Starter Submit Appsetting Result

Result: not performed.

Filtered starter appsetting presence before the hard stop:

| Setting | Present |
| --- | --- |
| `PUMPKIN_TENANT_ID` | no |
| `PUMPKIN_API_KEY` | no |
| `NEXT_PUBLIC_PUMPKIN_API_URL` | yes |
| `PUMPKIN_API_URL` | yes |
| `PUMPKIN_HOST_TENANT_ROUTES_JSON` | no |
| `PUMPKIN_PREVIEW_FIXTURE_ROOT` | no |

No starter appsetting mutation was performed because the handoff submit key returned `401` and no authenticated source-supported API-side key setup path was available.
