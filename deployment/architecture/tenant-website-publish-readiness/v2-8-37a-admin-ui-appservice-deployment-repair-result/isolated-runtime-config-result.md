# Isolated Runtime Config Result

Target:

`app-pumpkin-admin-isolated-centralus-001`

Only non-secret Admin UI runtime settings were set:

- `NEXT_PUBLIC_API_URL`
- `NODE_ENV`
- `SCM_DO_BUILD_DURING_DEPLOYMENT`
- `HOSTNAME`
- `PORT`
- `WEBSITES_PORT`

Startup command:

`node server.js`

No app settings list/show command was run.
