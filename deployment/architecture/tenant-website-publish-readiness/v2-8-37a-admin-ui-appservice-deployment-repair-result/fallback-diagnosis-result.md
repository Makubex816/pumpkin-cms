# Fallback Diagnosis Result

Fallback Kudu publish API attempt:

Not used.

Reason:

The corrected POSIX-entry ZIP deployed successfully through the primary App Service deployment path on the isolated Web App. Since isolated succeeded, no fallback publish method was needed.

Final fallback classification:

`fallback_not_required_primary_onedeploy_repair_succeeded`
