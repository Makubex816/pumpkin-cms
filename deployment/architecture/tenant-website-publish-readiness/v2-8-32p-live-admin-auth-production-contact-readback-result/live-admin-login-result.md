# Live Admin Login Result

Status: not run.

Reason:

The saved JWT path did not authorize readback, and the binding path could not run because `adminJwtSecretValue` was missing.

Login endpoint reserved for binding path:

`https://app-pumpkin-api-prod-centralus-001.azurewebsites.net/api/auth/login`

No admin password was printed or written.

No token or cookie was requested, printed, or written.

