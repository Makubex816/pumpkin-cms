# Future Provider Binding Plan

Provider/contact binding remains deferred.

Required future order:

1. Clear live health on the selected Central US host.
2. Confirm whether startup/auth configuration requires a secret binding or a code-level health bypass for unauthenticated health routes.
3. Only after health passes, request explicit approval for any provider/contact secret binding.
4. Only after provider binding is approved and completed, request explicit approval for contact POST validation.

No provider/contact secret was bound in H.
