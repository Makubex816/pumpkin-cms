# Canonical Pumpkin API URL

Selected H evidence URL:

`https://app-pumpkin-api-prod-centralus-001.azurewebsites.net`

Canonical status: do not promote for downstream binding yet.

Reason: the corrected deployment succeeded, but both approved health routes returned HTTP `500`. This URL remains the selected Central US target and canonical candidate for the next diagnostic phase, but it should not be used for live contact/provider binding until health passes.
