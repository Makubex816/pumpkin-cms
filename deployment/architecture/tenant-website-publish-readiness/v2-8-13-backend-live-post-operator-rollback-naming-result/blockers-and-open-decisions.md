# Blockers And Open Decisions

No blocker remains for future staging publish execution approval.

Open decisions for future phases:

- whether staging publish execution should use the latest sanitized `.tmp` output, a static publish dry-run release folder, or a separately generated artifact;
- where the SWA deployment token is stored outside the repo for the future operator;
- whether real email delivery or Pumpkin API persistence proof is required before live publication;
- when DNS, indexing, and live publication should be separately approved.

