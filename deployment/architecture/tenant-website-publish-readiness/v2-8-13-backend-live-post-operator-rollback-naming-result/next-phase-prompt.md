# Next Phase Prompt

Approve V2.8.14 Scoped Ice Staging Publish Execution Approval only: use the completed V2.8.13 backend verification result to perform the first scoped staging deployment to the resolved Azure Static Web App target `swa-ice-static-staging` in `rg-ice-static-staging` only if the exact artifact root, deployment method, deployment token storage outside the repo, operator, rollback/abort owner, abort criteria, and no-go checks pass. Deploy only the approved Ice static artifact to the staging Static Web App default host `happy-mud-0b375e20f.7.azurestaticapps.net`, then verify only the approved staging default-host routes `/`, `/contact`, `/service-areas`, `/sitemap.xml`, and `/robots.txt`. No DNS changes, no Search Console/indexing, no live publication, no production-domain cutover, no CMS writes, no provider writes, no Azure infrastructure mutation, no RBAC assignment, no protected config reads, no keys/listKeys, no connection strings, no SAS, no external crawling beyond approved staging-host route checks, no broad retries, and no generated `.tmp` artifact staging.

