# DNS Indexing Live Publication Closed Gates

Closed in V2.8.13:

- DNS mutation;
- Cloudflare mutation;
- Search Console/indexing;
- sitemap submission;
- live publication;
- production-domain cutover;
- external crawling or live-page crawl checks.

Backend verification does not authorize deployment, DNS, indexing, or live publication.

