# DNS Custom Domain Indexing Closed Gates

Status: closed.

V2.8.17A performed only read-only target/domain metadata checks and local/non-deploying validation.

Confirmed closed:

- DNS changes;
- custom-domain mutation;
- Search Console actions;
- indexing requests;
- live-publication actions outside the static deployment boundary;
- contact-form submission;
- contact endpoint POST;
- external crawling;
- outbound URL checks.

The existing production domains remain attached and `Ready`; no mutation was attempted.
