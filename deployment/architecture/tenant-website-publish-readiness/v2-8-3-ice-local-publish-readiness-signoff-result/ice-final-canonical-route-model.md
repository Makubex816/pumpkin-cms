# Ice Final Canonical Route Model

Final local canonical routes:

| Route | Source slug | Static output |
| --- | --- | --- |
| `/` | `home` | `out/index.html` |
| `/service-areas` | `service-areas` | `out/service-areas/index.html` |
| `/contact` | `contact` | `out/contact/index.html` |

Removed or non-canonical routes:

| Route | Final state |
| --- | --- |
| `/ice-rink-rentals` | removed from seed pages, fallback pages, and generated output |
| `/events-holiday-activations` | removed from seed pages, fallback pages, and generated output |

The static output directory check confirmed `out/ice-rink-rentals` and `out/events-holiday-activations` are absent.

