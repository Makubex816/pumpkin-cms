# Canonical Pumpkin API URL

Selected target URL:

`https://app-pumpkin-api-prod-centralus-001.azurewebsites.net`

Canonical future `PUMPKIN_API_URL` status: not promoted.

Reason: live `/health` and `/api/health` still returned HTTP `500` in V2.8.32I.

The URL remains the selected Central US target and canonical candidate for the next deploy-only health recovery phase.
