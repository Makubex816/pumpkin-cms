# Next Phase Prompt

Approve V2.8.55 Airstrip Las Vegas Raw Package Intake, Target-Domain Normalization, Public Secret Scan, Package Shape Detection, ZIP/Folder Rendering Feasibility Preflight, and No-Live-Mutation Validation only.

Use the hard-locked next tenant domain `airstripclublasvegas.com`. Do not create the tenant, deploy, mutate live records, mutate Azure/appsettings, mutate DNS/indexing, send contact POSTs, submit forms, upload media, delete resources, read protected configs, read owner hard-copy secrets, use keys/listKeys/SAS/connection strings, mutate the external SDI-AI repo, stage `.tmp`, or use `git add -A`.

The raw Airstrip package should be placed in an ignored intake location for inspection only. The phase must classify package shape, run public secret scanning, detect target-domain mismatches, decide whether exact frontend rendering is possible, and produce a go/no-go plan for static passthrough, source-build, Pumpkin conversion, or hybrid rendering.

Hard stop if the package contains secrets, protected config, unknown executable requirements, a conflicting tenant/domain, or any requirement for live mutation.

