# Updated Master Build Map

## P0 - Preserve Known Good Runtime

- Keep Ice/Pumpkin production and observability resources intact.
- Keep V2.8.54F do-not-delete register authoritative until superseded.

## P0 - Lock Airstrip Target

- Treat `airstripclublasvegas.com` as the next real tenant domain.
- Pause `strip-club-near-me-vegas` for live creation unless owner reconfirms it.

## P1 - Worktree Owner Decisions

- Execute exact-path owner decision batches only after approval.
- Commit completed report phases separately from source/platform changes.
- Defer content-review, source-like, protected-looking, and unknown-origin paths.

## P1 - Airstrip Package Intake

- Receive raw ZIP/folder into ignored intake.
- Secret scan and classify package shape.
- Normalize tenant/domain metadata to Airstrip.

## P1 - Rendering Feasibility

- Decide static passthrough, source build, Pumpkin conversion, or hybrid.
- Run local-only rendering proof before any tenant creation.

## P1 - Admin UI Cleanup

- Make `/dashboard/forms` the actual Leads/Form Entries area.
- Add `/dashboard/leads` redirect/alias or remove confusing nav.

## P2 - Backup Gap Closure

- Identity restore plan.
- Secret restore handoff plan.
- Live restore adapter isolated proof.

## P2 - Resource Cleanup Proof

- Prove OLM staging and legacy endpoint dependencies before deletion prompts.

## P3 - Controlled Airstrip Creation Preflight

- Only after package, rendering, backup, resource, and worktree gates are closed.

