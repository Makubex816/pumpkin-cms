# Media Backup Result

RBAC/login downloaded 302 unique canonical blobs totaling 28,343,976 bytes. All 302 are non-zero, all canonical names are unique, and every blob has a SHA-256. The 473 alias mappings are included. Storage keys, `listKeys`, connection strings, and SAS were not used.
