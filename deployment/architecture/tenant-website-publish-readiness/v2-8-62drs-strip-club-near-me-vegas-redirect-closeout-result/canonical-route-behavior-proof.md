# Canonical Route Behavior Proof

Both blocked source routes and both intended target routes exist exactly once in the 43-page Admin readback. The source files are moved-page stubs whose head metadata automatically navigates to the target and whose body links to it.

The normalized API page representation preserves the stub body but does not prove the source route automatically resolves to the intended target. The missing redirect therefore represents distinct behavior, not an accounting-only omission. No public route probe was run because the tenant is undeployed and launch-held.
