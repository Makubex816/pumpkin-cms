# Next Phase Prompt

Approve V2.8.2 Tenant Website static source refresh and sanitized publish package dry-run only: resolve the V2.8.1 local preflight blockers without deploying or publishing. Create a sanitized local build path that does not read `.env.local` or protected config, refresh or bind an approved non-secret Ice static source containing only `home`, `contact`, and `service-areas`, remove obsolete Ice route outputs, carry forward or explicitly revalidate the static form endpoint contract without secrets, run static validation, static output validation, staging package validation, Runtime QA, Resource Registry/provider-profile checks, OLM publish gate checks, artifact ignore checks, and produce a canonical V2.8.2 result package and root report. RollerRinkRentals.com must remain paused unless explicitly approved. No deployment, no DNS change, no Search Console/indexing, no live-page publication, no CMS writes, no production/provider writes, no Azure mutation, no RBAC assignment, no protected config reads, no keys/listKeys, no connection strings, no SAS, no external crawling/live outbound URL checks, and no generated artifacts staged into Git.
