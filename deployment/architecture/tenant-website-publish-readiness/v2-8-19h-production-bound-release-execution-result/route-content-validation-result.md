# Route Content Validation Result

Result: pass.

Selected artifact route checks:

| Route | File | Status |
| --- | --- | --- |
| `/` | `index.html` | Expected title, CTAs, public email, Azure media references, and `index, follow` metadata present |
| `/service-areas` | `service-areas/index.html` | Expected title/H1, availability review copy, public email, Azure media references, and `index, follow` metadata present |
| `/contact` | `contact/index.html` | Expected title/H1, public email, mailto link, form UI copy, Azure media references, and `index, follow` metadata present |

Static manifest:

- Page count: 3.
- Pages: `contact`, `home`, `service-areas`.
- All expected route files exist.

Local media references in checked route HTML: 0.

