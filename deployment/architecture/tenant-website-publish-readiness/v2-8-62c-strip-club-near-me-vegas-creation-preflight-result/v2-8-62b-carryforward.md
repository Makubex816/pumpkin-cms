# V2.8.62B Carryforward

V2.8.62B is committed at `81852fe6448aa24a3c54ad08b091a462d8e405a2` on `feature/admin-page-editor-import-export`.

Accepted carryforward:

- 43 source Page candidates;
- 10 club/detail Page subsets;
- 19 guide/article Page subsets;
- 473 physical media paths, 302 unique hashes, 150 hashes reached by the initial HTML-image scan, and 152 initially unreached hashes;
- 65 accepted form instances and 15 FormDefinition candidates;
- required consent, honeypot, and hidden context controls in every form candidate;
- one route initially quarantined after 45 apparent broken references;
- 45 route-effective Airstrip links held without any Airstrip request;
- V1 validator pass with 0 errors and 0 warnings;
- 8/8 inert static preview renders; and
- 39/39 GET-only runtime checks.

V2.8.62C used the 62B candidate as immutable input. It did not alter the compiled package during durable retention.

The owner fidelity addendum supersedes two 62B interpretations. Full static analysis expands dependency-backed media to 152 hashes. It also proves the 45 apparent broken references were a redirect-parser attribution error: the physical top-level route is a valid redirect and must be preserved. Two unrelated missing anchor targets are the actual link defects.
