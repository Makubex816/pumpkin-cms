# Package Fidelity Contract

Status: `adopted_as_hard_creation_gate`.

The supplied Strip Club Near Me Vegas front-end package is the visual and functional specification. The durable 62B/62C compiled package remains evidence, but it is not approved for live creation until it is regenerated and proved under the platform fidelity contract.

## Static Evidence

The fresh detached-DOM and static-source analysis completed at `2026-07-11T06:01:50.220Z` with:

- 43 physical HTML routes, including 3 immediate meta-refresh redirect routes;
- 1,747 physical link occurrences and 1,797 route-effective occurrences;
- 861 physical controls and 896 route-effective controls;
- 57 physical form elements and 65 route-effective instances;
- 473 media paths, 302 unique content hashes, and 157 duplicate-content groups;
- 50 inline/external script units inspected as text: 37 JSON-LD, 10 presentation configurations, and 3 interactive units; and
- 0 uploaded-script executions and 0 package network requests.

The source ZIP identity remains SHA-256 `c1612b09fa9d0e2629382957a8f943c7f43256e17b25bbb6b89eaf116e43c0f4`.

## Disposition Rules

| Source class | Current disposition |
| --- | --- |
| Valid direct routes and content | preserve or safely adapt with equivalent renderer/browser proof |
| Three meta-refresh routes | preserve redirect and target-page behavior |
| Valid internal/relative links | preserve target, query, fragment, label, and context |
| Intentional external venue links | preserve with safe external-link attributes and browser proof |
| 45 effective Airstrip links | preserve intentional source behavior; no probe; owner approval required for any later removal/replacement |
| Two links to missing anchors | blocked until target IDs or owner-approved equivalent behavior are repaired |
| 152 media hashes with dependency evidence | preserve or hash-deduplicate while retaining every alias |
| 150 hashes without dependency proof | blocked from exclusion until conclusive dead-asset proof or owner approval |
| 861 physical controls | safely adapt equivalent and browser-prove |
| Dormant JavaScript hooks | blocked owner review; neither activate nor discard silently |
| 65 effective form instances | blocked until the instance ledger preserves the full form contract |
| Generic starter fallback | prohibited |

## Compiler Corrections Required

The current durable artifact must not drive a live import without correction:

- it recommends an HTML-only 150-media scope, while the full graph finds 152 dependency-backed hashes and leaves 150 unresolved hashes requiring proof;
- it quarantines `/24-hour-late-night-strip-clubs-las-vegas`, although the physical file is a valid redirect to the intact guide;
- it labels form provenance as 62 physical forms plus 3 aliases, while the source has 57 physical forms and three redirect routes inheriting 4, 1, and 3 forms, for 65 effective instances;
- its 15 field-shape candidates collapse 65 instances that produce 32 full-contract signatures when labels, options, defaults, and submit behavior are included; and
- its instance map does not yet prove preservation of every submit label, hidden value, purpose, success message, and reset/navigation flow.

V2.8.62C did not mutate the source package or durable compiled output. These are required next-phase compiler/adapter corrections.

## Gate Result

`not_approved_for_live_tenant_creation_package_fidelity_browser_and_owner_gaps_pending`
