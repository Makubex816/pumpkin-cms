# Public FormDefinition Read Result

Approved public read count: `1`.

Public read:

- Endpoint: `GET /api/forms/ice-rink-rentals/definitions/v2-8-48-proof`
- Auth: tenant API key in memory only.
- Result: HTTP `200`.
- Returned ID matched `v2-8-48-proof-form`.
- Returned `formType` matched `v2-8-48-proof`.

The tenant API key was not printed or written.
