# Publish State And Required Approvals

Current page state:

| Page | Published | Include in sitemap |
| --- | --- | --- |
| `home` | false | false |
| `contact` | false | false |
| `service-areas` | false | false |

Required separate approvals before public preview:

- Approve Party Pros page publication or preview-only snapshot behavior.
- Approve Party Pros publish profile or starter-app preview adaptation.
- Approve any static snapshot/export command.
- Approve any deploy target.
- Approve any DNS/custom-domain or nameserver action.
- Approve any contact POST, form submission, or customer-facing POST proof.

Until those approvals are explicit, Party Pros remains a live CMS tenant with unpublished content only.

