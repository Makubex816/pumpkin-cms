# Next Phase Prompt

## V2.8.62F - Strip Club Near Me Vegas Shared Starter Immutable Preview Fixture, Full Browser Fidelity Proof, and One-Deploy Gate

Start from committed V2.8.62E. Freshly verify the backup manifest/checksum hashes, the 43/3/302/473/32/65/0 counts, the two accepted deviations, clean staging, and the unchanged Party Pros starter setting boundary.

Approved local focus when the owner starts this phase:

- implement the exact source scope in `v2-8-62f-preview-implementation-plan.md`;
- generate the ignored Vegas fixture from the V2.8.62E backup and hash-pinned CR assets;
- preserve all 43 routes, three redirects, 302 media records, 473 aliases, 32 FormDefinitions, 65 mappings, 45 effective Airstrip links, and 1,108 effective controls;
- execute only the audited Pumpkin behavior adapter, never uploaded package JavaScript;
- keep the age acknowledgement session-only and store no personal data;
- keep every form local no-post and create no FormEntry;
- run exact source tests, type-check, build, 172-route responsive proof, 12 redirect proofs, and non-Airstrip runtime no-regression;
- if and only if all local gates pass and the owner approves the live step, make at most one starter deployment attempt with protected config excluded.

Hard limits:

- no starter appsetting mutation;
- no Vegas runtime-key read, activation, rotation, or form use;
- no API/Admin/Ice/Airstrip deploy;
- no tenant, CMS, media, redirect, form, domain, import, or publish mutation;
- no DNS, nameserver, custom-domain, TLS, public publication, or indexing action;
- no contact POST, form submission, customer inquiry, or FormEntry;
- no Airstrip request or link-follow proof;
- no storage keys, listKeys, connection strings, or SAS;
- no secret/token/cookie/API-key printing;
- no fixture, package, media, screenshot, browser profile, `.tmp`, `.next`, `node_modules`, hardcopy, backup, or deployment ZIP staging;
- no `git add -A`.

Hard stops:

- stop on any backup/hash/count drift;
- stop if any route, link, media alias, form instance, visible control, redirect, title/H1, or responsive proof is missing;
- stop if any unaccepted blocked item or generic fallback remains;
- stop if a POST or Airstrip request would be required;
- stop if Party Pros or Ice regresses;
- stop after one failed deployment attempt and request new owner approval.

V2.8.62F does not approve a custom host or public launch. Retain V2.8.63A as the later multi-tenant identity, login-email change, TenantAdmin transfer, and tenant contact-email architecture phase.
