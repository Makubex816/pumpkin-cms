# Form Fidelity Readback

The database contains 32 complete-contract FormDefinitions and 65 page-level mappings. All mappings match the compiler ledger. Field identifiers normalized by the API compare equivalently after the documented lowercase/alphanumeric normalization; all labels, types, options, defaults, requirements, consent, honeypot, hidden metadata, routing, and success contracts remain represented.

All 32 definitions are draft `preview-no-post`; submission approval and FormEntry creation approval are false. FormEntries remain 0. V2.8.62E issued zero form submissions and zero contact POSTs.
