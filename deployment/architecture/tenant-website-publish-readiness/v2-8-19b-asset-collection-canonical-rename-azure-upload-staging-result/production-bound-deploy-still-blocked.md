# Production-Bound Deploy Still Blocked

Production-bound deploy remains blocked.

`swa-ice-static-staging` is production-bound because real custom domains are attached:

- `iceskatingrinkrentals.com`
- `www.iceskatingrinkrentals.com`

V2.8.19B did not deploy to `swa-ice-static-staging`.

V2.8.19B also did not deploy to `swa-ice-static-isolated-staging`. Isolated staging remains a later explicit approval boundary.
