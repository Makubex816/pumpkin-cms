# Future Provider Binding Plan

Provider/contact binding remains deferred until explicitly approved.

Recommended next order:

1. Approve a provider/app-setting binding phase for the canonical API URL.
2. Bind only the explicitly approved non-public provider/contact settings.
3. Verify only approved readiness endpoints or safe metadata.
4. Request a separate contact POST validation phase after binding is complete.

Canonical future `PUMPKIN_API_URL`:

`https://app-pumpkin-api-prod-centralus-001.azurewebsites.net`

No provider/contact secret was bound in J.
