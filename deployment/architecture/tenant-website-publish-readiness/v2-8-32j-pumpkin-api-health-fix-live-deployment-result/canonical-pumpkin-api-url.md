# Canonical Pumpkin API URL

Canonical future `PUMPKIN_API_URL`:

`https://app-pumpkin-api-prod-centralus-001.azurewebsites.net`

Promotion status: promoted.

Reason:

- The selected Central US Web App exists and is running.
- The V2.8.32I fixed artifact was deployed successfully.
- `/health` returned `200 OK`.
- `/api/health` returned `200 OK`.

Provider/contact binding remains a separate approval gate.
