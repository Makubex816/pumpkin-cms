# Next Phase Prompt

```text
Approve V2.8.19 Tenant Website Image-rich Recovery Implementation Planning only: use the V2.8.18 live-site regression investigation package as source evidence, keep `swa-ice-static-staging` classified as production-bound because `iceskatingrinkrentals.com` and `www.iceskatingrinkrentals.com` are attached, and use `swa-ice-static-isolated-staging` as the only safe staging target. Locate or assemble the approved image-rich source of truth for `/`, `/service-areas`, and `/contact`; produce a route/content/asset manifest with titles, H1s, sections, images, alt text, CTAs, form behavior, SEO metadata, owner approval fields, isolated-staging QA gates, rollback plan, and production-bound approval requirements. Do not deploy, redeploy, mutate Azure, change DNS, alter custom domains, reset/print/inspect tokens, read `.env.local` or protected config, query Key Vault secrets, call keys/listKeys, generate connection strings or SAS, submit contact forms, crawl production, run live outbound URL checks, run Search Console/indexing, publish live pages, copy random images into production, or stage unrelated files.
```

