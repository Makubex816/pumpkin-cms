# MediaAsset Retry Blob Public HTTP Result

Result: pass.

- HTTP probe tool: `curl.exe`.
- Public HTTP status: `200`.
- Fallback needed: no.

The public proof was captured against the same uploaded blob before cleanup.

