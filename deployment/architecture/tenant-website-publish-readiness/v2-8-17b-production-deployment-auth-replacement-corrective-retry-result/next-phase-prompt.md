# Next Phase Prompt

Approve V2.8.17C Production Deployment Failure Forensics And Tooling/Auth Remediation Plan only: use the completed V2.8.17B result package to investigate why the single approved SWA CLI production deployment attempt from the validated artifact root failed with exit code `1`. This phase may review non-secret local command output, SWA CLI version/tooling state, safe read-only Azure Static Web App metadata for `swa-ice-static-staging` in `rg-ice-static-staging`, and repo deployment scripts/docs. It must not run another deployment attempt, must not run a deployment dry-run that consumes or validates the deployment token, must not print/export/list/write/reveal the deployment token, must not read `.env.local` or protected config, must not use keys/listKeys, Key Vault secrets, connection strings, or SAS, must not mutate Azure infrastructure/configuration/app settings/RBAC/custom domains/DNS, must not run Search Console/indexing, must not crawl/follow outbound links, must not submit forms or POST to the contact endpoint, and must not perform CMS/provider writes. Produce a concise forensics/remediation result package, a root report, an updated tracker/SOT state, and exact next-phase approval wording for any future deployment retry only if the failure mechanism is understood.

