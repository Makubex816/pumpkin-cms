# Brand Theme Mapping Result

Brand target:

`Airstrip Club Las Vegas`

Theme target:

`airstrip-dark-v1`

Mapped source evidence:

- Logo references: `logo-full.svg`, `logo-wordmark.svg`
- Theme source files: `src/data/airstrip-theme.json`, `src/app/globals.css`, `tailwind.config.js`
- Observed CSS class prefix: `as-`
- Visual direction: dark nightlife/VIP theme with gold and red accents in the normalized package

Result:

`brand.json` and `theme.json` were generated in the outside-repo normalized package. Publication content and source-domain labels still need owner review before live creation.

