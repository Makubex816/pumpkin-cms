# Anonymous Container No-Listing Proof

An anonymous request to the Vegas container listing endpoint returned HTTP 404 with no blob enumeration. Individual known blob URLs remained readable.

Result: `passed_public_blob_read_no_listing`. Access is `blob`, never `container`.
