# DNS Indexing Live Publication Closed Gates

Closed in V2.8.12:

- DNS mutation;
- Cloudflare mutation;
- Search Console/indexing;
- sitemap submission;
- live publication;
- production-domain cutover;
- external crawling or live-page crawl checks.

The future backend POST approval does not imply deployment, DNS, indexing, or live publication approval.

