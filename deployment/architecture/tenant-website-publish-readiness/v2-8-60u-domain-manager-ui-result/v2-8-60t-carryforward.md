# V2.8.60T Carryforward

V2.8.60T completed DomainBinding backend/API/storage.

Carryforward:

- Pumpkin API DomainBinding routes are live.
- Cosmos container `DomainBinding` exists with partition key `/tenantId`.
- Airstrip DomainBinding exists as `pending_dns_records`.
- TenantAdmin denial proof already passed for DomainBinding API routes.
- DNS/custom-domain binding remains paused.

V2.8.60U used that backend and did not deploy Pumpkin API.
