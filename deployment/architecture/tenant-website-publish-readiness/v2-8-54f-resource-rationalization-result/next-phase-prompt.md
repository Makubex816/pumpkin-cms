# Next Phase Prompt

Approve V2.8.54G only after selecting one exact next lane. Options:

- exact owner-approved worktree commit/delete/archive resolution;
- read-only dependency proof for `rg-pumpkincms-stg-eastus-olm` cleanup candidates;
- legacy static form endpoint decommission proof;
- partner package read-only review;
- partner tenant creation preflight after repository and resource gates are closed.

Do not delete resources, stop resources, deploy, mutate appsettings, create tenants, mutate records, upload media, submit forms, send contact POSTs, mutate DNS/indexing, read secrets, stage temporary files, or use blanket staging without separate approval.
