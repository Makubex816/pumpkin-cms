# Next Phase Prompt

Approve V2.8.25 Contact Managed API Discovery Shape Investigation and Isolated-Only Retest only: use the completed V2.8.24 result showing that the prepared CommonJS Node v4 entrypoint deployed successfully to `swa-ice-static-isolated-staging` but POST `/api/static-contact` still returned HTTP 404 with an empty body for trace `v2-8-24-isolated-contact-20260625211731`. Investigate the SWA managed Functions discovery/package shape without deploying to production, without production contact POSTs, and without protected config reads. The phase may inspect public-safe source/package metadata, adjust only the isolated contact API package shape if needed, test a canonical SWA-compatible Node v4 entrypoint layout such as `package.json main=src/functions/*.js` or a root `index.js` that requires the registration file, run local function/package readiness checks, build a fresh sanitized static artifact, verify `/contact` still serializes `/api/static-contact`, verify the isolated target has no custom domains, run boolean-only token checks, deploy exactly once to `swa-ice-static-isolated-staging` with app-plus-API shape if all gates pass, and submit exactly one synthetic non-PII isolated POST only after successful deployment and preflight. Do not deploy to `swa-ice-static-staging`, do not deploy production, do not submit a production contact POST, do not retry after a sent isolated POST, do not mutate DNS/custom domains, do not mutate Azure except the approved isolated SWA app-plus-API deployment, do not mutate app settings, do not access protected config, do not print/list/reset/export deployment tokens, do not use keys/listKeys, do not generate connection strings or SAS, do not access inbox/provider systems, do not run Search Console/indexing, do not run arbitrary outbound URL checks, and do not use `git add -A`. Produce the V2.8.25 result package, root report, validation summary, and exact-path commit instructions.
