# HTTPS-Only Result

The shared starter App Service is running with `httpsOnly=true`.

This app-level setting is unchanged, but it cannot provide a valid Vegas HTTPS experience until both Vegas hosts are bound and receive matching certificates. No setting mutation occurred.
