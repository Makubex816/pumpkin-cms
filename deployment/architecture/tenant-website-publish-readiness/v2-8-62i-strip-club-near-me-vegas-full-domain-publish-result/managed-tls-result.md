# Managed TLS Result

Vegas managed-certificate count is 0 and Vegas SNI binding count is 0. No certificate issuance or SNI action was attempted.

Party Pros remains preserved with two hostname bindings and two distinct `SniEnabled` certificate thumbprints. Azure lists only the two Party Pros `Microsoft.Web/certificates` resources in the resource group.
