# Hostname Domain Dependency Result

Function App hostname inventory:

| Field | Result |
| --- | --- |
| Default hostname | `func-ice-static-contact-20260605.azurewebsites.net` |
| Custom hostname count | 0 |
| Hostnames | Default Azure hostname only |

Result: no custom-domain dependency found.

This result did not override the metric and storage blockers.
