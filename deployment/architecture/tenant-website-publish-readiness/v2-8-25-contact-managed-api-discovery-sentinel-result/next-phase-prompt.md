# Next Phase Prompt

Approve V2.8.26 Contact Production Remediation Release With Proven Managed API Compatibility Package only: use the completed V2.8.25 isolated-staging result showing that the v3-compatible `function.json` managed API package deployed to `swa-ice-static-isolated-staging`, `GET /api/static-contact-health` returned 200 with `ok: true`, and exactly one synthetic isolated POST to `/api/static-contact` returned 200 with `ok: true` for trace `v2-8-25-isolated-contact-20260626092010`. The phase may build a fresh sanitized Ice artifact, package the static app plus `deployment/static-azure/forms/static-form-endpoint-compat`, verify the production-bound SWA target `swa-ice-static-staging` and its custom domains read-only, run local compatibility package checks, run static contact artifact verification, run boolean-only deployment token checks, deploy exactly once to `swa-ice-static-staging` only if explicitly approved, then verify production `/contact` and production `/api/static-contact-health`. Production live contact POST must remain separately gated and may be sent exactly once only if this next approval explicitly includes it and production health succeeds. Do not deploy to any unapproved target, do not submit more than one production POST, do not retry after a sent POST, do not mutate DNS/custom domains, do not mutate app settings, do not access protected config, do not print/list/reset/export deployment tokens, do not use keys/listKeys, do not generate connection strings or SAS, do not access inbox/provider systems, do not run Search Console/indexing, do not run arbitrary outbound URL checks, and do not use `git add -A`. Produce the V2.8.26 result package, root report, validation summary, and exact-path commit instructions.
