# Future Provider-Binding Plan

Date: 2026-06-27

Provider binding remains future-only.

Required before provider binding:

- deployment issue diagnosed and corrected;
- ZIP deployment succeeds;
- `/health` succeeds;
- `/api/health` succeeds;
- separate protected provider-binding approval is granted.

Not performed in this phase:

- app settings list/show/set;
- provider secret binding;
- static contact secret binding;
- contact POST;
- FormEntry validation;
- Admin live API read validation.
