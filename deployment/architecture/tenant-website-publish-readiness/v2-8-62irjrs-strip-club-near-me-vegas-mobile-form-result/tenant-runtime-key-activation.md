# Tenant key activation

Only `PUMPKIN_TENANT_API_KEY__STRIP_CLUB_NEAR_ME_VEGAS` was temporarily activated. After failed readback it was deleted and the starter restarted. The final setting is absent.
