# Artifact Root Selection Result

Selected artifact root for the next scoped staging deployment boundary:

```text
apps/ice-rink-web/.tmp/sanitized-static-build/ice-rink-rentals/sanitized_20260612225811/repo/apps/ice-rink-web/out
```

Selection rationale:

- Generated by the repo-supported sanitized build path.
- Protected config was not copied into the sanitized workspace.
- Static output validator passed.
- Staging package validator passed.
- Required Ice routes are present: `/`, `/service-areas`, `/contact`.
- Required files are present: `sitemap.xml`, `robots.txt`.

This artifact remains under ignored `.tmp` output and must not be staged into Git.

