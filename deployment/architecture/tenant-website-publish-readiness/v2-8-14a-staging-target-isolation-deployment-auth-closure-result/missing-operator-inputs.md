# Missing Operator Inputs

Required before the next scoped deployment execution prompt:

```text
SWA_CLI_DEPLOYMENT_TOKEN
```

This value must be the deployment token for:

```text
swa-ice-static-isolated-staging
rg-ice-static-staging
kind-island-0a85a740f.7.azurestaticapps.net
```

The value must be present in the same terminal session. It must not be pasted into repo files, docs, scripts, screenshots, command-line arguments, or reports.

No other missing operator values were found for target isolation, tooling, artifact validation, or backend carryforward.

