# Next Phase Prompt

Approve V2.8.14B scoped Ice isolated staging deployment execution only: use the completed V2.8.14A target-isolation and deployment-auth closure package to deploy the validated sanitized Ice static artifact to the isolated non-production Azure Static Web App target `swa-ice-static-isolated-staging` in `rg-ice-static-staging` only if `SWA_CLI_DEPLOYMENT_TOKEN` is present in the same terminal session by presence-only check. Deploy only the approved artifact root `apps/ice-rink-web/.tmp/sanitized-static-build/ice-rink-rentals/sanitized_20260612225811/repo/apps/ice-rink-web/out` or a freshly regenerated artifact that passes the same validators. Use repo-supported pinned SWA CLI tooling and do not print or pass the deployment token value on the command line. After deployment, perform bounded route checks only against `https://kind-island-0a85a740f.7.azurestaticapps.net/`, `/service-areas`, `/contact`, `/sitemap.xml`, and `/robots.txt`, then document readback, rollback/abort posture, and result evidence. No production deployment, no deployment to `swa-ice-static-staging`, no DNS changes, no custom domain changes, no Search Console/indexing, no live-page publication, no external crawling beyond the bounded Azure default-host route checks, no outbound URL checks, no contact form submission, no POST to contact endpoint, no CMS writes, no provider writes, no app settings mutation, no RBAC assignment, no protected config reads, no deployment-token print/export/listing, no Key Vault secrets, no keys/listKeys, no connection strings, no SAS, and no generated artifacts staged into Git.

