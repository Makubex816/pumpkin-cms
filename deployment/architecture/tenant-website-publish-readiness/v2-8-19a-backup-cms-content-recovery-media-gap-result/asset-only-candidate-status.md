# Asset-only Candidate Status

Status: not an asset-only candidate.

The backup contains useful media metadata, but no image binaries. It also contains richer CMS pages and route/form/theme/SEO data, so treating it as asset-only would miss the main recovery value.

Correct classification:

- content source rebuild candidate
- media metadata recovery candidate
- binary gap manifest input

