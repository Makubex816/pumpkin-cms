# Next Phase Prompt

Approve V2.8.61OP Party Pros Email DNS Preservation and Manual Delegation Decision only.

Use V2.8.61OO carryforward:

- Azure DNS zone exists.
- Apex A, `www` CNAME, `asuid`, and `asuid.www` are staged.
- Target Azure nameservers are known.
- Public registrar delegation still points to Afternic.
- Manual nameserver change remains held pending email DNS export/migration or explicit owner no-email acceptance.
- No registrar mutation, hostname binding, TLS, deploy, publish, contact POST, form submission, Ice mutation, or Airstrip action occurred.

Next owner input needed:

- complete current DNS export or screenshots from the client DNS account; or
- explicit decision that this domain will not receive email and null-email/parked behavior is acceptable.

Do not log into Bluehost, mutate registrar DNS, change nameservers, bind Azure hostnames, create managed TLS, deploy/redeploy, publish Party Pros pages, submit forms, run contact POST, mutate Airstrip, mutate Ice, use storage keys/listKeys/SAS, read or print secrets/tokens/cookies, or stage generated artifacts unless explicitly approved.
