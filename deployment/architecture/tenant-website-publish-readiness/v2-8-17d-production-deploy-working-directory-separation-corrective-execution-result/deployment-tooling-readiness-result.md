# Deployment Tooling Readiness Result

Result: passed.

| Field | Value |
| --- | --- |
| Tool invocation | `npx --yes @azure/static-web-apps-cli@2.0.9` |
| Version check | `2.0.9` |
| Deployment command shape | `deploy --app-location app --output-location . --app-name swa-ice-static-staging --resource-group rg-ice-static-staging --env production --api-language none --api-version none --no-use-keychain --verbose=silly` |
| Dry-run | `false` |
| Keychain | `--no-use-keychain` |
| API language/version | `none` / `none` |

The deployment command was run from the neutral deployment parent, not from repo root and not from the artifact root.

