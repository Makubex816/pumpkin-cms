# Pumpkin Domain Metadata Association Proof

Readback at `2026-07-14T00:01:37Z` used the approved SuperAdmin hardcopy in memory and the required custom-header mode. No credential, header value, bearer token, cookie, or secret was printed or persisted.

| Field | Fresh readback |
| --- | --- |
| Tenant | `strip-club-near-me-vegas` / `Strip Club Near Me Vegas` |
| DomainBinding count | 1 |
| DomainBinding ID | `domainbinding-strip-club-near-me-vegas-stripclubnearmevegas-com` |
| Primary domain | `stripclubnearmevegas.com` |
| WWW domain | `www.stripclubnearmevegas.com` |
| Status | `pending_manual_launch_held` |
| DNS records in Pumpkin metadata | 0 |
| DNS validation | `pending` |
| TLS | `not_started` |
| Promotion | `not_promoted` |

The legacy tenant row leaves its optional domain fields empty. The held DomainBinding is the source-supported domain association and does not conflict with another tenant or domain. No Pumpkin metadata was changed.

Authentication accounting: one approved login POST refreshed the SuperAdmin `lastLogin` field. This was not a form, customer, content, tenant, domain, or publish mutation.
