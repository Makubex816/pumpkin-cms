# Live Health Check Result

Approved live health URLs:

- `https://app-pumpkin-api-prod-eastus-001.azurewebsites.net/health`
- `https://app-pumpkin-api-prod-eastus-001.azurewebsites.net/api/health`

Result: not attempted.

Reason: the Web App was not created and the ZIP was not deployed because App Service plan creation was blocked by Azure quota.

HTTP statuses: not available.

Public-safe response summaries: not available.

No arbitrary outbound URL checks were run.

