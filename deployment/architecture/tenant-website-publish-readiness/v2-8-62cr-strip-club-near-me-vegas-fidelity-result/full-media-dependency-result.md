# Full Media Dependency Result

The dependency graph spans HTML attributes, srcset, inline styles, CSS URLs, JavaScript strings, data attributes, JSON metadata, lazy images, and fallback behavior.

| Classification | Unique hashes | Disposition |
| --- | ---: | --- |
| required static | 120 | preserved |
| required dynamic | 32 | preserved and safely wired |
| optional owner asset | 150 | preserved, not silently excluded |
| conclusively dead | 0 | none |
| owner excluded | 0 | none |

All 473 physical files (39,521,491 bytes) are preserved as aliases to 302 canonical content hashes. All 302 canonical binaries were copied. The 12 basename-only metadata rows map to all matching preserved candidates when two distinct source files share a basename; this conservative one-to-many mapping avoids guessing or dropping either file.

No media upload, storage key, listKeys call, SAS, blob mutation, or container action occurred.
