# Anchor And Download Parity Result

The source contains 153 physical anchor links, 0 `mailto:` links, 0 `tel:` links, and 0 physical download links. Every anchor resolves after two narrowly scoped repairs:

| Route | Source defect | Repair | Result |
| --- | --- | --- | --- |
| `/guides/strip-club-scams-avoid-vegas-taxis-uber` | `#main` target absent | assigned `id=main` to the existing main element | passed |
| `/guides/vip-bottle-service` | `#pricing` target absent | assigned `id=pricing` to the existing source pricing section | passed |

The dormant source CSV-download code has no bound source control. It remains dormant and was not invented as a visible download action.
