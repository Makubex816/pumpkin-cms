# Google Indexing Deferred Carryforward

Google/Search Console/indexing remains deferred and hard-stopped.

V2.11.2:

- does not submit sitemaps;
- does not call Search Console;
- does not call URL Inspection API;
- does not call Google Indexing API;
- does not request indexing;
- rejects indexing-requested source fixtures.

Indexing remains outside this lane.
