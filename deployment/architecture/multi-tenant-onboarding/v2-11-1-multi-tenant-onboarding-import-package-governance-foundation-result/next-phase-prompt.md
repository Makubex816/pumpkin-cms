# Next Phase Prompt

Approve V2.11.2 Import Package Validator Hardening And Builder Planning only: harden the V2.11.1 local/no-write import package governance validator into a more complete package-local validator and builder plan, including stricter schema validation, cross-reference validation, fixture expansion, validation report format, no-write builder dry-run design, Admin/API read-only integration plan, package-local tests, result package, root report, and platform control doc updates. This phase may modify only the V2.11 multi-tenant onboarding governance implementation and docs needed for local/no-write validation. This phase must not create a live tenant, must not execute tenant imports, must not resume RollerRinkRentals.com, must not perform CMS/provider/MediaAsset writes, must not add live mutation endpoints, must not deploy or redeploy, must not change DNS or custom domains, must not run Google Search Console or indexing actions, must not submit sitemaps, must not crawl or follow outbound links, must not submit contact forms or POST to contact endpoints, must not mutate Azure infrastructure/configuration, must not assign RBAC, must not read protected config, must not use or print deployment/OAuth tokens, must not query Key Vault secrets, must not use keys/listKeys, must not generate connection strings, and must not generate SAS.
