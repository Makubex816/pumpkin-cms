# V2.11.3 Carryforward

V2.11.3 supplied the implementation contract:

- shared model `pumpkin.importIntakePreview.sharedModel.v1`;
- read-only envelope `pumpkin.importIntakePreview.readonlyApiEnvelope.v1`;
- 8 future GET-only API routes;
- DTO/read-model plan;
- endpoint-to-panel/detail mappings;
- provider modes and fixture fallback plan;
- no-go, rollback, paused tenant, and disabled action contracts;
- valid Ice and Roller read-only envelope fixtures;
- invalid enabled-action fixture.

V2.11.4 implemented that contract without changing the approved fixture semantics.
