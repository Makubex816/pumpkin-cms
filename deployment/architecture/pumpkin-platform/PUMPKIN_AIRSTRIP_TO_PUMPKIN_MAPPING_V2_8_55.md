# Pumpkin Airstrip To Pumpkin Mapping V2.8.55

Status: mapping_gap_report_created

Mapping summary:

- Tenant: needs generated `tenant-profile.json`; package tenant ID currently appears as `airstrip`.
- Domains: target `airstripclublasvegas.com`; source mentions `www.airstriplasvegas.com`, requiring normalization.
- Brand: logos and color palette are present; `brand.json` must be generated.
- Theme: `airstrip-theme.json` is convertible to Pumpkin Theme.
- Pages: 25 Next routes are convertible after baseline route decisions.
- Contact/forms: `/request-booking` reservation flow should become an Airstrip FormDefinition, not a generic static contact form.
- Media: 13 media assets can become a media manifest, with upload deferred.
- Users: no public users module detected; secure handoff needed.
- Publish/validation: missing and must be generated.

Creation readiness: not ready until package conversion, secure handoff, validation, and rendering proof are complete.

