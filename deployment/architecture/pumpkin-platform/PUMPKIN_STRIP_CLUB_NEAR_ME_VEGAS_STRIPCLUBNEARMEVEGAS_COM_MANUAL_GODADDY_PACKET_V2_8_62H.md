# Pumpkin Strip Club Near Me Vegas Manual GoDaddy Packet V2.8.62H

**HELD - NAMESERVER CHANGE NOT PERFORMED**

Tenant `strip-club-near-me-vegas` and domain `stripclubnearmevegas.com` are technically preprovisioned in Azure DNS. The customer/owner must perform any future GoDaddy action manually and only after explicit owner and adult/nightlife compliance launch authorization.

Current nameservers:

1. `ns49.domaincontrol.com`
2. `ns50.domaincontrol.com`

Future Azure nameservers:

1. `ns1-03.azure-dns.com`
2. `ns2-03.azure-dns.net`
3. `ns3-03.azure-dns.org`
4. `ns4-03.azure-dns.info`

All four Azure values are required. Do not enter only two.

After a separately approved manual change, notify the Pumpkin operator and preserve screenshots outside the repository. Do not edit individual GoDaddy records after Azure becomes authoritative. If rollback is approved, restore both current DomainControl values and record the timestamp against association ID `strip-club-near-me-vegas--stripclubnearmevegas-com`.

This packet does not approve public launch, hostname binding, TLS, publication, indexing, runtime-key activation, contact POST, or form submission.

The full operator packet is in `deployment/architecture/tenant-website-publish-readiness/v2-8-62h-strip-club-near-me-vegas-stripclubnearmevegas-com-azure-dns-result/manual-godaddy-nameserver-packet.md`.
