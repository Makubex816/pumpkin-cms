# Package Compiler Operator Action Packet V2.8.61D

The V2.8.61D compiler creates a non-technical owner packet inside each compiled package:

`OWNER_ACTION_PACKET.md`

For Airstrip, the packet explains:

- Pumpkin detected a Next.js App Router site.
- Hybrid server runtime proof is required.
- Routes, media, and reservation form candidate were mapped.
- Protected config filename findings require owner review, with contents unread.
- Tenant admin credential handoff is required separately and is not included.
- Responsive proof must pass before isolated preview, production deploy, domain cutover, contact POST, or customer-facing POST proof.

This packet is suitable for owner/operator review before any live import or launch approval.
