# Identity management production activation V2.8.63CRR

Activation remains held until the immutable API candidate passes same-App-Service login tests with dual-write disabled and enabled. Admin deployment and incremental management flags follow API stability. Tenant rename and provider-dependent delivery remain disabled.
