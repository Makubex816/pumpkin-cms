# Pumpkin Updated Master Build Map V2.8.54G

Status: planning_map_no_live_mutation

1. Preserve live Ice/Pumpkin runtime and observability.
2. Hard-lock `airstripclublasvegas.com` as the next true tenant target.
3. Do not use `strip-club-near-me-vegas` for live creation unless the owner reconfirms it.
4. Resolve owner-decision worktree batches by exact path.
5. Intake the Airstrip ZIP/folder package in an ignored no-mutation phase.
6. Prove frontend rendering feasibility before creation.
7. Clean Admin UI Forms/Leads navigation.
8. Close backup gaps for identity restore, secret restore, and live restore adapter.
9. Run resource cleanup dependency proof before any decommission prompt.
10. Request controlled Airstrip creation preflight only after all prior gates pass.

