# Pumpkin Tenant Credential Change And Transfer Standard V2.8.62DR

Credential rotation, global email change, tenant membership transfer, and business contact-email change are distinct operations. Membership transfer must verify the replacement, prove access, revoke only the affected old tenant membership, preserve unrelated memberships and audit history, and retain rollback evidence. Never merge identities automatically or replace a password unless explicitly approved.
