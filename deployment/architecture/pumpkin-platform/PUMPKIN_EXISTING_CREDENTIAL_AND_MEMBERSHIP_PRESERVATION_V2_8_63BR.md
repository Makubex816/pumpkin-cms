# Existing credential and membership preservation V2.8.63BR

Acceptance uses only existing approved credentials/sessions. It must not reset/change passwords, change email, invent credentials, or create/change memberships. Pre/post proof compares password fingerprints, login emails, roles, membership IDs/counts, tenant slugs, contacts, and notification settings. Tokens remain in process memory only.
