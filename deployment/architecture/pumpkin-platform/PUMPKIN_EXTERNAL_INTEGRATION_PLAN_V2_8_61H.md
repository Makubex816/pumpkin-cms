# Pumpkin External Integration Plan V2.8.61H

Status: recommended staged plan.

1. Owner decision: resume Airstrip cutover on current runtime or keep cutover paused for upstream integration first.
2. Contract-only planning: route, DTO, role, model, and package diff.
3. API/model adaptation: preserve active aliases and operational endpoints.
4. Admin UI adaptation: port useful form designer/user/theme work into the standalone Admin UI.
5. Starter-template lane: evaluate `apps/starter-app` in isolation for future tenant generation.
6. Public form proof: only after explicit customer-facing POST/form-submit approval.

Do not combine DNS cutover, upstream merge, API migration, and public form submission proof in one phase.
