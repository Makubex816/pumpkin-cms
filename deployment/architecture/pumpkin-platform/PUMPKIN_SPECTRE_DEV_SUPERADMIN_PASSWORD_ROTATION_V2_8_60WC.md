# Pumpkin Spectre Dev SuperAdmin Password Rotation V2.8.60WC

Date: 2026-07-05

## Status

Documentation blocked on hardcopy TXT checksum mismatch.

The direct operator redacted proof reports that Spectre Dev SuperAdmin password rotation completed outside Codex:

- old password rejected;
- new password login succeeded;
- role remained `SuperAdmin`;
- tenants, users, and domain bindings access returned HTTP 200;
- no deploy/domain/contact/form action occurred.

Codex did not perform password rotation in this documentation phase.

## Blocker

The computed hardcopy TXT SHA-256 did not match the prompt-provided expected TXT SHA-256. The JSON hardcopy SHA-256 matched.

No hardcopy contents were read or copied.

