# Identity-management activation standard

Activate management only after immutable API promotion passes production acceptance. Use a synthetic non-customer identity for mutation proofs. Preserve customer passwords, emails, memberships, TenantAdmins, tenant identifiers, and contacts. Email-delivery features may remain provider-held; tenant rename remains a separate phase.
