# Identity management activation recovery V2.8.63CR

Activation remains gated until all required routes are implemented, the Admin UI is functional read-only before mutations, both production login roles meet the timeout, customer fingerprints/digests are preserved, and synthetic-only reversible workflow proofs pass. Tenant rename remains deferred to V2.8.63D. Verified email, invitation, forgot-password, and security-notice delivery remain held until the owner selects and configures a provider.
