# Production-parity slot identity promotion V2.8.63CRR

Identity candidates must be built once, hash-recorded, deployed to a slot on the production App Service, and swapped without rebuilding only after bounded role-login acceptance. Basic plans must be scaled to a slot-capable tier before validation. Preserve the pre-cutover production slot and never substitute an independent canary for same-App-Service proof.
