# Pumpkin Tenant CMS Visual Persistence Decision V2.8.61OSH

Decision: retain the accepted Party Pros runtime/fixture visual baseline temporarily and defer CMS persistence to a separately approved phase.

The later phase should convert the accepted page structures, catalog blocks, site chrome, theme tokens, metadata, FormDefinition binding, and media references into authoritative tenant CMS/package output. It must compare CMS rendering against the accepted OSG baseline, retain rollback evidence, and avoid switching runtime source until owner-approved parity is proven.

Future tenant readiness should require a visual reference when available, safe inventory without executing uploaded code, structured compilation, responsive proof, functional form proof, and explicit owner acceptance.

OSH CMS mutation count: 0.

