# CRS stream-interruption resumption standard

Reconcile Git, Azure state, artifacts, backups, flags, and production runtime before mutation. Treat semantically equivalent commits as completed work. Reuse the deployed candidate before rebuilding. Never promote an unidentified artifact. Preserve unrelated work and require an empty staging area after focused commits.
