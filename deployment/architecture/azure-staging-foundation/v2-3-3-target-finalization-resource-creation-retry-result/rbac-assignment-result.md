# RBAC Assignment Result

No RBAC assignments were created.

| Required RBAC input | Status |
| --- | --- |
| Principal | not explicitly reviewed for assignment |
| Role | not explicitly reviewed |
| Scope | staging scope exists, but no role assignment approval target was supplied |
| Assignment command | not run |

The managed identity `id-pumpkincms-olm-stg` exists and can be used in a future RBAC approval. Until a role assignment is explicitly approved and verified, `OLM_STAGING_RBAC_OR_AUTH_MODE` and `OLM_STAGING_IDENTITY_OR_SESSION_TYPE` remain unresolved for execution.

