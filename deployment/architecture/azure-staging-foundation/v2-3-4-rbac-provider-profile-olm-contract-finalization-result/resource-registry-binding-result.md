# Resource Registry Binding Result

Resource Registry binding candidate created:

```text
resource-registry-binding-candidate.json
```

The candidate includes:

- staging resource group and location
- Cosmos account, endpoint host, database, containers, and partition key
- storage account and staging evidence containers
- managed identity metadata
- Cosmos RBAC assignment metadata
- diagnostics resources
- secret boundary flags

No Resource Registry service write was performed. The candidate is committed metadata only and contains no credential values.

