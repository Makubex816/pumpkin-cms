# RBAC Assignment Result

No RBAC assignments were created.

| Required RBAC input | Status |
| --- | --- |
| Principal | missing |
| Role | missing |
| Scope | missing/future candidate only |
| Assignment command | not run |

V2.3.1 defines RBAC boundaries and identity options, but does not explicitly define a reviewed principal, role, and scope. The prompt allows RBAC only when those values are explicitly reviewed, so RBAC remained blocked.

