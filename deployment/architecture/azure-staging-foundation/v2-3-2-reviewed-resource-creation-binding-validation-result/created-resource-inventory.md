# Created Resource Inventory

No resources were created in V2.3.2, so there is no created-resource inventory.

Candidate resource group read-only check:

| Resource | Exists | Source |
| --- | --- | --- |
| `rg-pumpkincms-stg-eastus-olm` | no | `az group show --name rg-pumpkincms-stg-eastus-olm --output json` |

Existing resources outside this candidate group were not mutated or adopted as OLM staging targets.

