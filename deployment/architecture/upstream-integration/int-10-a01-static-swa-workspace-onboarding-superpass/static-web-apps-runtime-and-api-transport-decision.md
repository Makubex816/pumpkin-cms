# Static Web Apps runtime and API transport decision

The accepted local foundation produces static files suitable for Azure Static Web Apps. Public dynamic behavior is represented only as a non-credential tenant-scoped API transport contract.

Cost and live-mutation hold:

- Any paid Static Web Apps plan, API backend linking, custom-domain binding, DNS/nameserver change, TLS action, or live form persistence proof requires separate owner approval.
