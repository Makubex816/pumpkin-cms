# Root npm workspace and lockfile result

Checkpoint status: pending source mutation.

Expected initial workspace members:

- apps/starter-app
- packages/pumpkin-ts-models
- packages/pumpkin-block-views

The downstream root workspace is a downstream release artifact and is not represented as part of the frozen upstream source. The existing root lockfile is not yet the INT-10 authoritative workspace lock at this checkpoint.
