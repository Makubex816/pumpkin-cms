# Next phase prompt

Do not begin V2.8.63D. Resume V2.8.63CRR at the true same-App-Service slot login hard gate. Preserve production rollback, slot `crr-validation`, deployment logs, and candidate hash. Diagnose why the legacy `User` email query does not return in a newly started production slot even with Gateway mode and bounded cancellation. Do not swap or activate features until invalid, SuperAdmin, and TenantAdmin slot logins pass with dual-write off and on.
