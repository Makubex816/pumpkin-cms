# Resume prompt

Obtain owner approval to scale `asp-pumpkin-api-prod-centralus-001` from S1 capacity 2 to S2 capacity 2. Do not change capacity without that approval.

After approval: verify production first; scale; start `crr-validation`; confirm deployment `cb31ebcf-0b01-4215-9521-af10d4edd8d1` and SHA-256 `04edf05c...72b482c`; run cold and repeated bounded diagnostic, invalid login, both existing-role logins, dual-write and exact-audit proof; then perform immutable promotion and continue Admin feature activation. Keep tenant rename, indexing, Airstrip public runtime, and provider creation out of scope.
