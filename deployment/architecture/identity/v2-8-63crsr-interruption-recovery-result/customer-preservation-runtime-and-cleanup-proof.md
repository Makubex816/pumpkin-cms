# Customer preservation and cleanup

No customer password, email, role, membership, TenantAdmin, tenant identifier, contact setting, DNS, TLS, CMS, form, or indexing value was changed. No Airstrip public-runtime request was made. Locator backfill was additive.

After the capacity gate, slot dual-write was disabled and the slot was stopped. Production then passed three health requests and both existing-role logins in about one second each.
