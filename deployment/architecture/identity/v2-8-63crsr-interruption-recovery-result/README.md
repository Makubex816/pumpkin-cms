# V2.8.63CRSR result

Status: `blocked_true_hard_gate_production_worker_or_capacity_owner_approval_required`.

The interrupted Git state was reconciled, the retained candidate was re-proved, and a bounded cold-worker retry correction was committed. Production is healthy on the unchanged rollback artifact. The validation slot is stopped with dual-write disabled. Promotion and management activation are held because two S1 workers reached sustained 99% CPU and up to 86% memory during required two-role login repetition; increasing plan cost requires owner approval.

Minimum requested recovery capacity: S2, capacity 2. This retains two workers while doubling per-worker CPU and memory relative to S1. Re-run slot cold/repeated Stage A and Stage B proof before any promotion.
