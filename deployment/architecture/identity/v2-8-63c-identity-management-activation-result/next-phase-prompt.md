# Resume prompt

Resume V2.8.63C from `blocked_three_corrected_api_deployments_failed`. Preserve the pre-activation backup and rollback deployment. Do not enable management flags. In an isolated production-equivalent environment, reproduce the login writer stall introduced by session-version lookup, capture server-side Cosmos timing/exception evidence, and prove legacy login plus dual-write/audit without changing customer credentials. Obtain a new explicit deployment-attempt budget before another production API replacement. Complete all original 63C stages before generating 63D.
