# Next Runtime Wiring Or Data Seed Preflight Prompt

```text
Approve Phase 2F-12J Ice Cosmos runtime profile wiring preflight only: use the Phase 2F-12I provider metadata endpoint and runtime profile bridge foundation to create the safe runtime wiring execution plan for Ice/Pumpkin Cosmos, including managed identity/RBAC requirements, configuration key names without values, local-dev versus live-Azure profile behavior, endpoint verification steps, abort/rollback rules, data seed/migration prerequisites, Backup Center connector readiness gates, and the next explicit runtime wiring execution prompt. No CMS runtime switch execution, no CMS writes, no data migration, no seed/import, no database export/import, no Cosmos document export, no keys/listKeys, no connection strings, no SAS, no protected config reads, no Azure mutation, no deployment, no Search Console/indexing, and no live-page publication.
```

