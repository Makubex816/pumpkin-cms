# Next Ice Full Backup Execution Prompt

Approve Phase 2F-8 Ice full standard backup execution only: after required env vars are present in the same terminal/session that launches Codex, run presence-only env checks printing only `PRESENT` or `MISSING`, then create a local standard Backup Center bundle for IceSkatingRinkRentals.com full website plus approved database backup evidence/export mode under an ignored local output path.

The execution scope may include read/export of Ice CMS tenant/site/page/route/form/SEO/redirect/theme/MediaAsset metadata, approved database backup artifact or platform backup evidence, approved media inventory and separately approved blob copy/download mode, static evidence, redacted config inventory, manifest, checksums, validation report, restore instructions, and restore validation dry-run report.

Required boundaries:

- standard backup only;
- no secret values in standard backup;
- no protected config reads;
- no real escrow payload;
- no secret export;
- no CMS writes;
- no MediaAsset writes;
- no restore into real systems;
- no deployment;
- no Azure/Cloudflare/DNS changes unless explicitly named as read-only evidence collection;
- no email/Microsoft 365 work;
- no Search Console/indexing;
- no live-page publication;
- no raw `content-review` staging;
- no ignored generated output staging.

The execution must stop if required env presence checks fail, if the output path is unsafe, if database/media/storage approvals are incomplete, if any secret-like value appears in standard backup output, or if restore validation fails.

Do not proceed to encrypted escrow execution without a separate owner-approved real escrow prompt.
