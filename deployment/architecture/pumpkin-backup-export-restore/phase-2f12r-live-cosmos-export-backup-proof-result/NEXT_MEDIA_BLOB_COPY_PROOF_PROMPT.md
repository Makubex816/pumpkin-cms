# Next Media Blob Copy Proof Prompt

Approve Phase 2F-12S media blob copy proof and full backup restore-readiness validation only: use the Phase 2F-12R Ice standard backup candidate and live Cosmos export proof as the database baseline, then perform a safe read-only/list-and-copy media blob proof for the approved Ice media scope into ignored `.tmp` output only, using approved identity/RBAC access if available without keys/listKeys, connection strings, or SAS generation. Integrate copied media proof into a new Ice standard backup candidate, run manifest/checksum/validator checks in production restore proof mode if all required components are present, run restore-plan validation, and create a result package.

No CMS runtime switch, no CMS writes, no MediaAsset writes, no deployment, no Search Console/indexing, no live-page publication, no protected config reads, no storage keys/listKeys, no connection strings, no SAS generation, and no generated `.tmp` artifacts staged into Git.
