# Blockers Or Warnings

No blocker prevented Phase 2F-12R completion.

Warnings:

- Ice is not fully backupable today because media blob full-copy proof is still pending.
- Restore-plan validation is database-proof complete but production restore proof remains partial.
- CMS runtime remains not switched to Cosmos.
- Live pages were not affected.
- The `.tmp` live export and backup candidate contain tenant backup data and must remain ignored and unstaged.
