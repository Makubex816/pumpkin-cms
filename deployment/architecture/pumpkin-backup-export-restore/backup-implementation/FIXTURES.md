# Fixtures

## Answer Fixtures

- `fixtures/tenant-standard-backup.answers.json`
- `fixtures/platform-standard-backup.answers.json`

## Data Fixtures

- `fixtures/fake-cms-content.json`
- `fixtures/fake-media-inventory.json`
- `fixtures/fake-static-evidence.json`
- `fixtures/fake-config-inventory.redacted.json`

## Fixture Rules

Fixtures are fake and non-secret. They use placeholder tenant/site IDs, placeholder hashes, redacted config values, and no real credentials.

Do not replace these fixtures with production data without a separate approval.
