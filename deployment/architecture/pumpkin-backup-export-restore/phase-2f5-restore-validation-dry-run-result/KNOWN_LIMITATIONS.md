# Known Limitations

The Backup Center is still not production-ready.

Remaining future work:

- encrypted secret escrow prototype;
- real database exporter/importer;
- real CMS/API exporter/restore adapter;
- real media/blob exporter/restore adapter;
- production backup packaging;
- API/Admin UI integration;
- job worker model;
- production audit logging and retention enforcement;
- real IceSkatingRinkRentals.com backup proof and restore drill.

Restore validation is dry-run only and never writes a real target.
