# Inventory Comparison Result

The restore dry-run compares actual fake bundle counts against `fixtures/restore-expected-counts.json`.

Compared counts:

- tenants;
- sites;
- pages;
- routes;
- forms;
- SEO entries;
- redirects;
- theme/settings entries;
- media asset entries;
- static evidence routes;
- config inventory entries.

The tenant fixture expects the single `example-tenant` scope. The platform fixture expects the full fake platform inventory.
