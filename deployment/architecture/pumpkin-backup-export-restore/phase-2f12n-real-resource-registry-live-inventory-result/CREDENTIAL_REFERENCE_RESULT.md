# Credential Reference Result

Committed credential reference input fixture:

`deployment/architecture/pumpkin-backup-export-restore/resource-registry-implementation/fixtures/credential-references.real-readonly-current.fixture.json`

Credential reference count:

- 9

Credential categories:

- Azure operator read-only login reference
- Cosmos managed identity runtime reference
- Ice media storage RBAC reference
- Static Web App deployment credential reference, not collected
- Function App platform settings reference, values not read
- Microsoft Graph mail app credential reference, values not read
- Cloudflare API credential reference, values not read
- Roller Rink Rentals API key reference, encrypted-vault eligible
- Pumpkin Admin JWT session reference, excluded-session-token

No credential reference contains plaintext values.
