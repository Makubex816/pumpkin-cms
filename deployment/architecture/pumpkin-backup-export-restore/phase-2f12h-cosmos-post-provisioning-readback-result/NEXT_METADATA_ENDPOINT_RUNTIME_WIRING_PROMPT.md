# Next Metadata Endpoint Runtime Wiring Prompt

```text
Approve Phase 2F-12I Ice Cosmos metadata endpoint and runtime wiring preflight only: use the Phase 2F-12H readback and provider resolver refresh result to design and prepare the safe non-secret metadata endpoint/runtime wiring plan for Ice/Pumpkin Cosmos, including required managed identity/RBAC decisions, CMS configuration names without values, environment/profile separation, provider resolver metadata endpoint response, local-dev versus live-Azure behavior, rollback/abort rules, validation gates, and the next execution approval prompt. No CMS runtime switch execution, no CMS writes, no data migration, no seed/import, no database export/import, no Cosmos document export, no keys/listKeys, no connection strings, no SAS, no protected config reads, no deployment, no Search Console/indexing, and no live-page publication.
```

This is a planning/preflight prompt only. Runtime wiring execution needs a later explicit approval.

