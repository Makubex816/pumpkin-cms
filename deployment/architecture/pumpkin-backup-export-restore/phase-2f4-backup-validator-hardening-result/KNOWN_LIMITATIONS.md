# Known Limitations

The Backup Center is still not production-ready.

Remaining future work:

- restore validation flow;
- encrypted secret escrow flow;
- real database exporter;
- real CMS/API exporter;
- real media/blob exporter;
- production backup packaging;
- API/Admin UI integration;
- job worker model;
- production audit logging and retention enforcement;
- real IceSkatingRinkRentals.com backup preflight.

The validator is stricter but still uses local contract checks and heuristic secret detection.
