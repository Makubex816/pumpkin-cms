# Fake Secret Catalog Result

Added fake escrow fixtures:

- `fixtures/fake-escrow-request.json`
- `fixtures/fake-escrow-policy.json`
- `fixtures/fake-escrow-recipient.json`
- `fixtures/fake-secret-catalog.json`

The catalog contains placeholder values only. No real secret, protected config, API credential, JWT, token, connection string, storage key, private key, or cookie was read or exported.
