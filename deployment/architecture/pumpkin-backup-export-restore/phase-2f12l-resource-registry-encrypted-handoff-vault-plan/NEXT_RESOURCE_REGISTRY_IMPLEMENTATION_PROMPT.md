# Next Resource Registry Implementation Prompt

Approve Phase 2F-12M Resource Registry local implementation foundation only: implement the local redacted Resource Registry foundation under the Backup Center/resource registry package, including schema-backed registry models, placeholder-only templates, registry validator, tenant/resource/runtime profile mapping, credential reference records without values, Backup Center integration hooks, CLI validation commands, docs, tests, and a result package. No secret collection, no key export, no encrypted vault creation, no protected config reads, no Azure/CMS/API calls, no CMS writes, no database export, no Cosmos export, no media/blob download, no deployment, no Search Console/indexing, and no live-page publication.

