# Next Database Connector Preflight Or Implementation Prompt

Approve Phase 2F-13 Ice database-provider source resolver implementation/preflight only: implement or plan a non-exporting Backup Center source resolver for IceSkatingRinkRentals.com that can identify the live database provider from presence-only env hints, owner-confirmed Azure subscription/resource group/account scope, read-only Azure/Cosmos metadata, and a future safe CMS/API provider metadata endpoint that returns no secrets or tenant payloads. Include validation rules, report output, abort conditions, tests/fixtures if implemented, and a go/no-go recommendation for later live database connector execution.

No database export, no Cosmos document export, no blob/media download, no storage keys/listKeys, no SAS generation, no protected config reads, no secret export, no CMS writes, no Azure mutations, no Cloudflare/DNS/deployment/email/Search Console actions, no escrow payload, and no live-page publication.
