# Plugin and Extension Design

This design defines safe tenant-scoped extension packs for future Pumpkin CMS capabilities.

First version is not a public marketplace and does not allow arbitrary code drop-ins.

