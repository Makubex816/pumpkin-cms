# Test Result

Initial local test result:

```text
npm test
6 tests passed
```

Initial local check result:

```text
npm run check
node --check passed for validator source files
6 tests passed
```

Example validation result:

```text
npm run validate:example
Validation passed: 0 error(s), 0 warning(s).
```

Final validation results are also recorded in the root Phase 2A-1 report after the complete local check pass.
