# Deployment Profile Registry

Deployment profiles describe supported production flows for tenants.

A profile defines infrastructure expectations, validators, deployment steps, smoke tests, approval gates, rollback steps, unsupported actions, and indexing final gate behavior.

