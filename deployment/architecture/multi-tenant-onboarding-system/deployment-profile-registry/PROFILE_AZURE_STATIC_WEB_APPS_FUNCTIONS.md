# Profile: azure-static-web-apps-functions

Azure-centered static profile.

Expected components:

- Pumpkin CMS
- static Next export
- Azure Static Web Apps
- Azure Functions for forms or APIs
- Azure-managed custom domains
- optional Azure media storage

Cloudflare may be absent. DNS provider steps must be profile-specific.

Validators must confirm SWA custom domains, Function endpoint CORS, route output, media URLs, and rollback.

