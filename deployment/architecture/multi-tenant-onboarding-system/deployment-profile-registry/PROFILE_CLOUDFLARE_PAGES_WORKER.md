# Profile: cloudflare-pages-worker

Cloudflare-centered static profile.

Expected components:

- Pumpkin CMS
- static export
- Cloudflare Pages
- Cloudflare Workers
- Cloudflare DNS
- profile-managed form endpoint or external form provider

Validators must confirm Pages deployment, Worker routes, media delivery, DNS records, cache behavior, and strict tenant route scoping.

