# Wizard Save and Resume Model

The wizard should support:

- draft save
- resume by tenant
- incomplete field status
- validation status per step
- owner assignments
- decision log
- support packet export

Secret values should not be shown after save. Prefer presence flags such as `configured` instead of storing visible values.

