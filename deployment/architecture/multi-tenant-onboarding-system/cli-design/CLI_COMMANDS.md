# CLI Commands

Future commands:

```text
pumpkin tenant intake init
pumpkin tenant validate-intake
pumpkin tenant validate-import-package
pumpkin tenant dry-run-export
pumpkin tenant media preflight
pumpkin tenant staging preflight
pumpkin tenant cutover preflight
pumpkin tenant final-review
pumpkin tenant indexing-final-gate
```

Mutation commands should be added later only after validators and approval handling are mature.

