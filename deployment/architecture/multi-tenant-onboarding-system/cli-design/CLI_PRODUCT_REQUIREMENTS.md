# CLI Product Requirements

The CLI should:

- initialize tenant intake folders
- validate intake
- validate import packages
- run dry-run exports
- produce redacted validation reports
- require explicit flags for mutation gates
- log safely with redaction
- support CI validation
- block Search Console/indexing until final approval

