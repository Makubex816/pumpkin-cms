# Import Package Spec

This folder defines the intended import-ready package for a new Pumpkin CMS tenant.

The package is designed to be created from human intake, validated locally, reviewed by an operator, and imported into CMS only after explicit approval.

No import package should contain secrets, local protected paths, raw image binaries, generated static artifacts, JWTs, API keys, deployment tokens, or connection strings.

