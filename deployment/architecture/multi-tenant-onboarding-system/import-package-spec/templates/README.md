# Example Import Package Templates

These files show the shape of a generic tenant import package. They use `example.com`, `media.example.com`, and placeholder IDs.

Do not replace placeholders with real secret values. Runtime-only fields such as `TENANT_API_KEY_RUNTIME_ONLY` are labels, not values.

