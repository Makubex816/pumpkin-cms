# New Tenant Intake Package

Use these templates to collect business and launch information before building an import-ready JSON package.

These forms are safe for non-secret information. Do not write real tenant API keys, tokens, passwords, connection strings, Azure credentials, Cloudflare credentials, Microsoft 365 passwords, or deployment tokens in these files.

Runtime-only fields may name the kind of secret required, but the value must remain outside docs and git.

