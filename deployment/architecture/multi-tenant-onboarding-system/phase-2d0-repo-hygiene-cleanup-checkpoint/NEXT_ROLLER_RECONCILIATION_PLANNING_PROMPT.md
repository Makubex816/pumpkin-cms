# Next Roller Reconciliation Planning Prompt

Use this only after the Phase 2D-0 hygiene checkpoint is reviewed.

```text
Approve Phase 2D-1 Roller CMS reconciliation planning only: using the committed Phase 2C-6B read-only evidence and the Phase 2D-0 repo hygiene checkpoint, create a no-write reconciliation plan for the existing active Roller tenant, existing published/sitemap-included Roller CMS pages, missing service-areas route, active theme state, noindex/sitemap state, and form-recipient registry gap. Work only from local repo files and approved read-only evidence. Do not perform CMS writes, tenant creation, MediaAsset writes, POST/PUT/PATCH/DELETE requests, Azure/Cloudflare/DNS/deployment/email/Search Console/indexing actions, protected config reads, secret printing, generated artifact staging, raw content-review staging, or live-page publication.
```

This is not approval to import content, write CMS records, deploy, or publish live pages.
