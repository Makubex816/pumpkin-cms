# Implementation Batches

## 2H-3: Local Scanner And Registry Foundation

Scope:

- local Node package;
- fixtures;
- scanner pipeline;
- URL normalizer;
- registry/instance proposal writer;
- schema validation;
- local tests.

No database, CMS writes, Admin/API, renderer changes, or crawling.

## 2H-4: Data Model And Local Persistence Foundation

Scope:

- richer local persistence;
- contract versioning;
- TypeScript model generation or manual types;
- C# mapping notes.

No production migration.

## 2H-5: Rendering-Control Prototype

Scope:

- local snapshot contract;
- deterministic rendering test harness;
- no live page publication.

## 2H-6: Backup/Onboarding/Tenant Bundle Integration

Scope:

- extend local backup fixture format;
- import package validation;
- tenant bundle outbound link files.

## 2H-7: API/Admin Implementation Planning Refresh

Scope:

- exact API controller/service tickets;
- exact Admin screen/component tickets;
- permission/audit details.

## 2H-8: Ice Read-Only Outbound Link Inventory Preflight

Scope:

- local or approved live-readonly inventory only;
- no writes or publication.
