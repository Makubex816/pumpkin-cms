# Next Phase 2H-3 Local Scanner Registry Foundation Prompt

Approve Phase 2H-3 Outbound Link Manager local scanner and registry foundation implementation only: implement a local/offline fixture-only scanner package under `deployment/architecture/outbound-link-manager/local-implementation/` that reads safe local fixtures, import package fixtures, tenant bundle fixtures, and backup bundle fixtures; extracts outbound URLs without external crawling; normalizes URLs; creates proposed tenant/site-scoped outbound link registry and instance records; validates records against the Phase 2H schemas; writes scan reports under ignored `.tmp`; includes fixtures and tests for no links, duplicate URLs, disabled global links, disabled instances, blocked domains, stale instances, and invalid URLs; and produces a result package. No database migration, no CMS writes, no API calls, no external link crawling, no protected config reads, no Azure/CMS/API mutations, no deployment, no Search Console/indexing, no live-page publication, and no generated `.tmp` artifacts staged into Git.
