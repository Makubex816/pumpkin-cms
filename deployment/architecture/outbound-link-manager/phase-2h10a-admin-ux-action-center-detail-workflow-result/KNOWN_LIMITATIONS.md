# Known Limitations

- Action Center counts are fixture-backed.
- Broken and suspicious labels are local mock classifications, not live checks.
- The detail drawer is read-only and does not persist decisions.
- Phase 2H-9 GET endpoints are not wired into this Admin runtime yet.
- Authorization remains inherited from the existing dashboard shell.
- Write actions require a future preflight and approval phase.

