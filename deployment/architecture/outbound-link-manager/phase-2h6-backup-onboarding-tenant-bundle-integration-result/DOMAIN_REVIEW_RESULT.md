# Domain Review Result

Domain review reports are generated for:

- tenant bundle export: `outbound-links/EXTERNAL_DOMAIN_REVIEW.md`
- onboarding import output: `external-domain-review.md`

Reports include:

- domain;
- local review status;
- link count;
- link statuses.

The reports are generated from local store and policy state only. No domain crawling or live HTTP checks occurred.
