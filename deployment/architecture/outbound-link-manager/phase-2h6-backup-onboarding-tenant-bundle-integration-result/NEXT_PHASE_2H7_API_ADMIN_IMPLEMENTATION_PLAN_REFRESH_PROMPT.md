# Next Phase 2H-7 API Admin Implementation Plan Refresh Prompt

Approve Phase 2H-7 Outbound Link Manager API/Admin implementation plan refresh only: after the controlled milestone repo remediation pass is complete, refresh the implementation plan for Pumpkin API and Admin integration using the completed local scanner, local store, rendering-control prototype, Backup Center export, tenant bundle export, onboarding import, and restore simulation contracts. Define exact API/Admin module boundaries, schema/migration plan, permissions, audit behavior, UI views, validation gates, tests, rollout sequence, and hard stops. No implementation, no database migration, no CMS writes, no Admin UI/API code changes, no external crawling, no live HTTP checks, no protected config reads, no Azure/CMS/API mutations, no deployment, no Search Console/indexing, and no live-page publication.
