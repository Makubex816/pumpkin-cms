# Known Limitations

- Phase 2H-22 is a package-builder and preflight phase only.
- The provider mode remains `staging-simulated`.
- Runtime QA evidence is the Phase 2H-21 local source/route harness reference, not a browser trace.
- Staging target identifiers are placeholders until a future explicit approval provides approved non-secret target identifiers.
- The first real staging-provider write is not performed until a later boundary phase.
- Production database migration and production writes remain blocked.

