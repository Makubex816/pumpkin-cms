# First-Write Package Refresh Result

Status: refreshed by scoped adapter evidence.

The existing first-write package still validates and remains the approved 48-record source package. V2.2.2 adds a new ignored `.tmp` live execution evidence package generated by the scoped Azure Cosmos adapter.

Evidence path:

```text
deployment/architecture/outbound-link-manager/local-scanner-registry-implementation/.tmp/v2-2-2-azure-cosmos-staging-execution/
```

The package linkage passed for:

- approval manifest ID `olapprove_508df3f03faa4f80`
- first-write batch ID `olbatch_b08e184fdc6565aa`
- expected record count `48`
