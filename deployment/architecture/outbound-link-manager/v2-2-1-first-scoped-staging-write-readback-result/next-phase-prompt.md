# Next Phase Prompt

```text
Approve V2.2.2 Outbound Link Manager Azure Cosmos staging data-plane executor/readback adapter and first-write retry only: implement the minimal repo-supported Azure Cosmos NoSQL staging provider adapter needed for the scoped OLM first-write path, using Azure Identity/RBAC only and the finalized provider profile olm-staging-cosmos-nosql-v1. The adapter must preserve local/offline and staging-simulated modes, must keep production-runtime blocked, must not use keys/listKeys, connection strings, SAS, protected config, Key Vault secret reads, or secret export, and must support create/upsert behavior only for the approved first-write batch olbatch_b08e184fdc6565aa with /tenantKey partitioning. Add readback verification, conflict/duplicate stop behavior, trace/audit/rollback evidence persistence, tests with fake/local providers, and exact live execution gates. After implementation and only if all gates pass, execute only the approved 48 OLM staging records against pumpkincms-olm-staging, then run immediate readback. Stop before writing if provider profile, RBAC propagation, Backup Center evidence, Resource Registry binding, approval manifest, batch ID, record count, tenant/site scope, readback, rollback, or no-go checks fail. No Azure infrastructure mutation, no RBAC assignment, no production database migration or production writes, no CMS writes outside the scoped OLM staging provider operation, no protected config reads, no keys/listKeys, no connection strings, no SAS, no deployment, no indexing, and no live-page publication.
```
