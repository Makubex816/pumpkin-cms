# Link Detail Result

The link detail route shows:

- URL
- registry id
- tenant and site keys
- domain
- policy status
- render action
- first and last detected timestamps
- instance count
- disabled reason when present
- usage instances
- active policy summary

Approve, disable, and related controls are disabled.

