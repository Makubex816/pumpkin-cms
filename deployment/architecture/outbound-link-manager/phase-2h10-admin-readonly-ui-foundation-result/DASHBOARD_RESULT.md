# Dashboard Result

The dashboard shows:

- link count
- instance count
- policy count
- audit event count
- pending review count
- disabled link count
- blocked domain count
- stale instance count
- local read-only data mode
- Backup Center, onboarding, and tenant-bundle status

All values come from the local fixture provider.

