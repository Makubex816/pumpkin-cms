# Staging-Simulated Provider Result

The staging-simulated provider store was written under:

```text
.tmp/phase-2h20-staging-persistence-integration/execution/staging-provider-store/
```

The store contains production-shaped entity collections for outbound links, instances, policies, scan runs, audit logs, render decisions, review decisions, bulk actions, rollback plans, and trace logs.

Live-readonly and live-write-approved profiles were both tested and blocked by design.

