# Test Result

Command:

```powershell
npm run check
```

Result: passed.

Test summary:

- 15 tests passed.
- 0 tests failed.

Covered:

- no-link fixture;
- single-link fixture;
- duplicate URL handling;
- same URL across multiple pages;
- tenant bundle scanning;
- unreviewed import domain;
- disabled global link;
- disabled specific instance;
- blocked domain;
- stale instance;
- URL normalization;
- malformed validator output rejection;
- output outside `.tmp` rejection;
- CLI scan/validate/inspect;
- source scan for external HTTP client and protected config read patterns.
