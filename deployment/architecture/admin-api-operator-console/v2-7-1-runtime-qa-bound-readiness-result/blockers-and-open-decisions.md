# Blockers And Open Decisions

Open blockers:

- Runtime QA upload to `runtime-qa-staging` remains blocked by missing Storage Blob data-plane RBAC.

Open decisions:

- Whether V2.7.2 should expand the operator console to Backup Center, Resource Registry, tenant onboarding, and provider readiness views.
- Whether a future phase should request Storage Blob data-plane RBAC for Runtime QA evidence upload.
- Whether a future write approval should enable another scoped OLM staging operation.

No V2.7.1 local readiness blocker remains.
