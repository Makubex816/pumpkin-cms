# Homepage Frontend Preview Checklist

Preview URL checked:

```text
http://localhost:3002/
```

Result:

- reachable: yes
- status/error: 200
- response contains likely homepage terms: yes

If the browser still shows old content, refresh the local frontend route and confirm the public app is reading the local CMS API rather than cached/fallback content.
